import { Category } from '../../shared/types/category.enum';

export const PRODUCT_SORT_OPTIONS = ['count', 'price'];
export const PRODUCT_SORT_DIRECTIONS = ['asc', 'desc'];
export const PRODUCT_CATEGORIES = Object.values(Category);
